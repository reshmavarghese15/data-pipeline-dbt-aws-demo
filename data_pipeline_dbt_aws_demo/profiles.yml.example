data_pipeline_dbt_aws_demo:
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: ./demo.db  # relative path inside the repo
      schema: 'main'
      schemas_and_paths:
        main: './demo.db'
      schema_directory: '/my_project/data'

  target: dev